import React from 'react';
import styles from './Footer.css';

export const Footer = () => (
  <div className={styles.footer}>
    <div className={styles.title}>This is the Footer!</div>
  </div>
);

export default Footer;
